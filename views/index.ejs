<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  
    <title>Document</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">

        <div class="form px-10 py-10">

            <form action='/create' method='post'>
                <input class="block px-5 py-3 w-full mt-2 rounded-lg bg-zinc-800" type="text" placeholder="task title here.." name="title">
                <textarea class="block px-5 py-3 w-full mt-2 rounded-lg bg-zinc-800 resize-none" placeholder="task details here,," name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-3 py-3" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-4 flex-wrap
            <% if(files.length>0){ %>
                <% files.forEach(function(val){%>
                    <div class="tak w-72 px-3 py-3 bg-zinc-800 rounded-md">
                        <h1 class="text-white text-2xl tracking-tight">Your Task Goes here</h1>
                        <a class="text-blue-500 mt-3 inline-block" href="">Read More</a>
                    </div>

                <% }) %>
            <% } else{ %>    
                <h3 class="text-zinc-600">No Tasks Yet</h3>
            <% } %>    


            
           

        
            
            
    
            
        </div>
    </div>

    
</body>
</html>